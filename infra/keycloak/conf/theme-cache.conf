# Merge into Keycloak conf/keycloak.conf (not gitignored secrets).
# Lets the browser keep theme CSS/JS/images for 30 days.
# Restart Keycloak after changing this. start-dev may still send no-cache
# until you add the same flag to the start command.
spi-theme-static-max-age=2592000
