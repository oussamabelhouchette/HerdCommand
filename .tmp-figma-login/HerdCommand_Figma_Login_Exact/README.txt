HerdCommand exact login HTML

Source Figma frame: 3:6 (login-ar), 1440 x 1024.

Open index.html in a modern browser. At 1440 x 1024 the layout dimensions match the Figma frame exactly.

Note: the background image and SVG icons are referenced from Figma MCP asset URLs. Figma states these URLs are temporary (approximately 7 days). For a production build, export/download those assets into a local /assets directory and replace the URLs.
